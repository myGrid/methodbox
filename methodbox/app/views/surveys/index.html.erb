
<ul class="sectionIcons">
  <!--<li><%#= icon "new", new_survey_path, "Upload New Survey", nil, "Upload New Survey" %></li>-->
  <!-- <li><%#= icon "view-all", all_files_path, nil, nil, "View All Files" %></li> -->
</ul>
<div id="survey_selector">
<%# form_remote_tag(:update => "table", :url => '/surveys/view_variables',:complete =>"Element.toggle('survey_selector');Element.toggle('variable_selector')") do -%>
  <% form_tag('/surveys/view_variables') do -%>
    <b style="padding-left:7px;">Select one or more surveys to search</b><br/><br/>
      <div style="padding-left:7px;"><%= submit_tag 'Search selected surveys' %></div><br/><br/>


  <% if @surveys.public_methods.include?("page_count") %>
    <!-- only do the page links if the collection supports it -->
    <%= render :partial => "layouts/paginate", :locals => { :collection => @surveys } -%>
  <% end %>

      <%= render :partial => "assets/resource_list", :locals => { :collection => @surveys, :authorization_for_showing_already_done=>true } %>

  <% if @surveys.public_methods.include?("page_count") %>
    <!-- only do the page links if the collection supports it -->
    <%= render :partial => "layouts/paginate", :locals => { :collection => @surveys } -%>
  <% end %>
  <br/><br/><br/>
      <div><%= submit_tag 'Search selected surveys' %></div>
    <% end -%>
</div>
<%#*<div class="fold" id="variable_selector" style="display:none;">%>
  <%#*<div class="foldTitle">Variables for selected Data Sets</div>%>
  <%#*<div class="foldContent">%>
    <%#*<div class="show_basic">%>
        <%#*<div id="table">%>
          <%#= render :partial => "view_variables", :locals => { :collection => @variables } %>
      <%#*</div>%>
    <%#*</div>%>
  <%#*</div>%>
<%#*</div>%>
