Hi <%= @name %>,

The protential user <%= @person.name %> just signed up requesting access but the system is currently set with REGISTRATION_CLOSED = closed.
He/She left the following message:

"<%= @message %>"

This account will need to be approved or rejected.
Doing this is still under development.

- The MethodBox Team -

-- end --

<%= render :partial => "disclaimer" -%>
