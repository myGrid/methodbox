# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
development:
  adapter: mysql
  database: methodbox_development
  username: ian
  password: mysqlthr@sh(r2
  socket: /var/mysql/mysql.sock

test:
  adapter: mysql
  database: methodbox_test
  username: ian
  password: mysqlthr@sh(r2
  socket: /var/mysql/mysql.sock

production:
  adapter: mysql
  database: methodbox_production
  username: ian
  password: mysqlthr@sh(r2
  socket: /var/mysql/mysql.sock

# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
#development:
#  adapter: sqlite3
#  database: db/development.sqlite3
#  timeout: 5000 

#test:
#  adapter: sqlite3
#  database: db/test.sqlite3
#  timeout: 5000
#
#production:
#  adapter: sqlite3
#  database: db/production.sqlite3
#  timeout: 5000
