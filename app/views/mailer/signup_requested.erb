Hi <%= @name %>,

The potential user <%= @person.name %>, <%= @person.user.email -%> just signed up requesting access but registration is currently closed.  An admin will need to check their account and approve or reject it.

He/She has the following description:

"<%= @message %>"

- The MethodBox Team -

-- end --

<%= render :partial => "disclaimer" -%>
